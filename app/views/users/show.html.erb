<div class='row'>


  <% if logged_in? %>
    <%= link_to 'Log out', logout_path, method: 'delete', class:'btn btn-default' %>
  <% end %>

  <div class="row">
    <div class='col-md-4 col-md-offset-4 form'>
      <h2> Welcome <%= @user.username %>! </h2>
        <%= render 'tasks/new' %>

        <h3> Today's Tasks </h3>
        <%= render 'tasks/daily_view' %>
    </div>
  </div>

</div>